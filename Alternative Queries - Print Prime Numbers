;WITH CTE AS
(SELECT 1 ID
UNION ALL
SELECT ID +1 FROM CTE
WHERE ID<1000)
SELECT * FROM CTE C1
WHERE NOT EXISTS (SELECT 1 FROM CTE C2
WHERE C2.ID >1 AND C2.ID*C2.ID<=C1.ID
AND C1.ID%C2.ID=0)
OPTION (MAXRECURSION 1000)
